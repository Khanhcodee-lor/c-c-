/*
 * BÀI 5: STRUCT, UNION VÀ ENUM CLASS
 *
 * Yêu cầu:
 * 1. Định nghĩa `enum class DeviceStatus { OFF, ON, ERROR }`.
 * 2. Định nghĩa struct "SensorData" gồm: ID (int), Value (float), Status (DeviceStatus).
 * 3. Viết hàm in ra thông tin của SensorData. Nếu Status là ERROR, in màu đỏ (nếu có thể) hoặc cảnh báo.
 * 4. Sử dụng Union để tách 1 biến float thành 4 byte uint8_t và in giá trị từng byte.
 */

#include <iostream>
#include <cstdint>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
