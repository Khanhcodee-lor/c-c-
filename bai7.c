/*
 * BÀI 7: HÀM, CON TRỎ HÀM VÀ STATE MACHINE
 *
 * Yêu cầu:
 * 1. Viết 4 hàm: add(a, b), sub(a, b), mul(a, b), div(a, b).
 * 2. Tạo một mảng con trỏ hàm chứa 4 hàm trên. 
 * 3. Nhập 2 số và 1 lựa chọn (0-3), gọi hàm tương ứng từ mảng để in kết quả.
 * 4. (Nâng cao) Mô phỏng 1 máy trạng thái đơn giản cho cửa tự động: 
 *    States: CLOSED, OPENING, OPEN, CLOSING.
 *    Nhập lệnh (1: Open, 2: Close) và in ra tiến trình chuyển trạng thái.
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
