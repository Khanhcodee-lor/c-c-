/*
 * BÀI 3: CON TRỎ VÀ THAM CHIẾU (POINTERS & REFERENCES)
 *
 * Yêu cầu:
 * 1. Viết hàm swap(int &a, int &b) dùng tham chiếu (reference) để hoán đổi 2 số.
 * 2. Viết lại hàm swap(int *a, int *b) dùng con trỏ.
 * 3. Khai báo 1 mảng động `int *arr = new int[5]`. Nhập dữ liệu và in ra mảng. 
 *    Sau đó giải phóng bộ nhớ bằng `delete[]`.
 * 4. Tại sao volatile quan trọng khi làm việc với thanh ghi phần cứng được ánh xạ vào bộ nhớ?
 */

#include <iostream>
#include <cstdint>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
