/*
 * BÀI 5: STRUCT, UNION VÀ ENUM
 *
 * Yêu cầu:
 * 1. Định nghĩa struct "SmartLight" gồm: ID (uint8_t), Độ sáng (uint8_t), Trạng thái (Enum: ON/OFF).
 * 2. Tính kích thước (sizeof) của struct này. Thử thêm thuộc tính uint32_t Power và xem kích thước thay đổi thế nào.
 * 3. Định nghĩa 1 Union "DataConverter" để chuyển đổi 1 số uint32_t thành mảng 4 byte (uint8_t[4]).
 *    Nhập 1 số Hex 32-bit (VD: 0x12345678) và in ra giá trị của từng byte.
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
