/*
 * BÀI 4: MẢNG (ARRAYS) VÀ CHUỖI (STRINGS)
 *
 * Yêu cầu:
 * 1. Nhập mảng n số nguyên. Viết hàm đảo ngược mảng đó (in-place) bằng con trỏ.
 * 2. Nhập 2 chuỗi s1 và s2. Viết hàm nối s2 vào sau s1 mà không dùng thư viện string.h.
 * 3. Tìm phần tử lớn nhất và nhỏ nhất trong mảng bằng 1 hàm duy nhất 
 *    (Gợi ý: truyền tham chiếu qua con trỏ cho max và min).
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
