/*
 * BÀI 2: CÁC PHÉP TOÁN THAO TÁC BIT (BITWISE OPERATIONS)
 *
 * Yêu cầu:
 * 1. Nhập 1 số nguyên uint16_t n.
 * 2. Thực hiện các thao tác sau và in kết quả dưới dạng Hex (dùng std::hex):
 *    - Set bit 3 của n.
 *    - Clear bit 0 của n.
 *    - Toggle bit 5 của n.
 * 3. Kiểm tra xem bit 7 của n là 0 hay 1. In ra "Bit 7 is 1" hoặc "Bit 7 is 0".
 * 4. Sử dụng std::bitset (nếu biết) để in số n dưới dạng nhị phân.
 */

#include <iostream>
#include <cstdint>
#include <iomanip>
#include <bitset>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
