/*
 * BÀI 1: KIỂU DỮ LIỆU VÀ THƯ VIỆN cstdint
 *
 * Yêu cầu:
 * 1. Nhập một số nguyên n (kiểu long long).
 * 2. Kiểm tra và in ra:
 *    - Nếu n nằm trong khoảng của int8_t, ép kiểu sang int8_t và in ra.
 *    - Nếu không, in "Ngoai pham vi int8_t".
 * 3. Tương tự với uint16_t (0 đến 65535).
 * 4. Khai báo 1 biến uint8_t x = 255. In ra giá trị static_cast<int>(++x). 
 *    Giải thích tại sao phải dùng static_cast ở đây và hiện tượng xảy ra với x.
 */

#include <iostream>
#include <cstdint>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
