/*
 * BÀI 7: LAMBDA EXPRESSION VÀ FUNCTION POINTERS
 *
 * Yêu cầu:
 * 1. Sử dụng con trỏ hàm để tạo một menu lựa chọn phép toán (+, -, *, /).
 * 2. Viết lại các phép toán trên bằng `std::function` hoặc `Lambda expression`.
 * 3. Sắp xếp một mảng số nguyên theo thứ tự giảm dần bằng hàm `std::sort` và một Lambda làm tiêu chí so sánh.
 */

#include <iostream>
#include <vector>
#include <algorithm>
#include <functional>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
