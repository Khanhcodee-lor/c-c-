/*
 * BÀI 2: CÁC PHÉP TOÁN THAO TÁC BIT (BITWISE OPERATIONS)
 *
 * Yêu cầu:
 * 1. Nhập 1 số nguyên uint8_t n (0-255).
 * 2. Thực hiện các thao tác sau và in kết quả dưới dạng Hex (0x%X):
 *    - Set bit 3 của n.
 *    - Clear bit 0 của n.
 *    - Toggle bit 5 của n.
 * 3. Kiểm tra xem bit 7 của n là 0 hay 1. In ra "Bit 7 is 1" hoặc "Bit 7 is 0".
 * 4. (Nâng cao) Nhập thêm số m. Tính n XOR m mà không dùng toán tử ^ (dùng &, |, ~).
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
