/*
 * BÀI 6: INLINE FUNCTIONS VÀ MACROS
 *
 * Yêu cầu:
 * 1. Viết một `inline function` tính Max của 2 số. So sánh ưu điểm của nó với Macro.
 * 2. Viết Macro tính chu vi đường tròn `CIRCLE_PERIMETER(r)` với PI = 3.14.
 * 3. Sử dụng `#if` và `#else` để biên dịch code dựa trên phiên bản phần mềm (VD: VERSION 1 hoặc VERSION 2).
 */

#include <iostream>

#define PI 3.14

int main() {
    // Code của bạn ở đây
    
    return 0;
}
