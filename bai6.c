/*
 * BÀI 6: PREPROCESSOR VÀ MACROS
 *
 * Yêu cầu:
 * 1. Viết Macro tính Bình phương của một số: SQUARE(x). 
 *    Lưu ý: SQUARE(2 + 3) phải ra 25, không phải 11.
 * 2. Viết Macro SET_BIT(reg, bit), CLEAR_BIT(reg, bit), READ_BIT(reg, bit).
 * 3. Sử dụng chỉ thị #ifdef để viết code: Nếu có #define DEBUG thì in ra "Debug mode is ON", 
 *    ngược lại không in gì cả.
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
