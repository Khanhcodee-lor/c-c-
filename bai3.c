/*
 * BÀI 3: CON TRỎ (POINTERS) VÀ TỪ KHÓA volatile
 *
 * Yêu cầu:
 * 1. Viết hàm swap(int *a, int *b) để hoán đổi giá trị 2 số nguyên.
 * 2. Khai báo 1 biến float f = 10.5. Dùng con trỏ kiểu uint8_t* để truy cập 
 *    và in ra giá trị của từng byte trong bộ nhớ của biến f.
 * 3. Khai báo một biến "volatile uint8_t status". Viết một vòng lặp giả lập 
 *    chờ biến status trở thành 1 (status == 1). 
 *    Tại sao trong lập trình nhúng, status phải có từ khóa volatile?
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
