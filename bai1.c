/*
 * BÀI 1: KIỂU DỮ LIỆU VÀ THƯ VIỆN stdint.h
 *
 * Yêu cầu:
 * 1. Nhập một số nguyên n (kiểu long long).
 * 2. Kiểm tra và in ra:
 *    - Nếu n nằm trong khoảng [-128, 127], ép kiểu sang int8_t và in ra.
 *    - Nếu không, in "Ngoai pham vi int8_t".
 * 3. Tương tự với uint16_t (0 đến 65535).
 * 4. Khai báo 1 biến uint8_t x = 255. In ra giá trị (x + 1). Giải thích hiện tượng xảy ra.
 */

#include <stdio.h>
#include <stdint.h>

int main() {
    // Code của bạn ở đây
    
    return 0;
}
